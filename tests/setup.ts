// Setup global pour tous les tests Vitest.
// Importer ici les mocks / polyfills globaux si nécessaire.

// En-tête vide intentionnel : aucun setup global requis pour les tests
// unitaires purs (score, cited, utils, diff, hash, sanitize).
// Les tests d'intégration utiliseront leurs propres beforeAll/afterAll.
